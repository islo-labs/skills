[job]
name = "qa"
version = "1.0.0"
description = "Three parallel black-box QA agents against a deployed app URL"

[job.params.qa_base_url]
type = "string"
required = false
default = "https://your-app.example.com"
description = "Deployed web app URL to test"

[run]
fail_fast = false
fanout = true
concurrency = 3
timeout = 7200

[run.sandbox]
mode = "provision"
image = "ghcr.io/islo-labs/islo-runner:latest"
snapshot_name = "qa"
gateway_profile = "default"
environment = "qa"
vcpus = 2
memory_mb = 4096
disk_gb = 20
internet_enabled = true

[run.sandbox.env]
ISLO_BASE_URL = "{{qa_base_url}}"
QA_RUN_ID = "{{run_id}}"

[[run.tasks]]
name = "qa-agent-web-core"

# Each fanout task provisions its own sandbox, so the prompts checkout repeats
# per task rather than running once for the job.
[[run.tasks.steps]]
name = "checkout-prompts"
exec = [
  "bash",
  "-lc",
  '''
set -euo pipefail
DEST=/workspace/.islo-prompts/REPLACE_WITH_REPOSITORY
git config --global url."https://x-access-token:$GH_TOKEN@github.com/".insteadOf "https://github.com/"
if [ -d "$DEST/.git" ]; then
  git -C "$DEST" fetch --depth 1 origin HEAD
  git -C "$DEST" checkout -f FETCH_HEAD
else
  mkdir -p "$(dirname "$DEST")"
  git clone --depth 1 https://github.com/REPLACE_WITH_OWNER/REPLACE_WITH_REPOSITORY.git "$DEST"
fi
''',
]

[[run.tasks.steps]]
name = "explore"
workdir = "/workspace/qa-harness"

[run.tasks.steps.run_agent]
mode = "session"
harness = "claude"
model = "claude-sonnet-4-5"
prompt = { type = "literal", value = """
Read and follow /workspace/.islo-prompts/REPLACE_WITH_REPOSITORY/.islo/prompts/web-core.md.
""" }

[[run.tasks.steps]]
name = "stage-findings"
exec = [
  "env",
  "QA_AGENT_ID=qa-agent-web-core",
  "QA_BRIEF_LABEL=Web core: login, navigation, and primary user flows",
  "python3",
  "/opt/qa-harness/harness/stage.py",
]

[[run.tasks]]
name = "qa-agent-web-platform"

[[run.tasks.steps]]
name = "checkout-prompts"
exec = [
  "bash",
  "-lc",
  '''
set -euo pipefail
DEST=/workspace/.islo-prompts/REPLACE_WITH_REPOSITORY
git config --global url."https://x-access-token:$GH_TOKEN@github.com/".insteadOf "https://github.com/"
if [ -d "$DEST/.git" ]; then
  git -C "$DEST" fetch --depth 1 origin HEAD
  git -C "$DEST" checkout -f FETCH_HEAD
else
  mkdir -p "$(dirname "$DEST")"
  git clone --depth 1 https://github.com/REPLACE_WITH_OWNER/REPLACE_WITH_REPOSITORY.git "$DEST"
fi
''',
]

[[run.tasks.steps]]
name = "explore"
workdir = "/workspace/qa-harness"

[run.tasks.steps.run_agent]
mode = "session"
harness = "claude"
model = "claude-sonnet-4-5"
prompt = { type = "literal", value = """
Read and follow /workspace/.islo-prompts/REPLACE_WITH_REPOSITORY/.islo/prompts/web-platform.md.
""" }

[[run.tasks.steps]]
name = "stage-findings"
exec = [
  "env",
  "QA_AGENT_ID=qa-agent-web-platform",
  "QA_BRIEF_LABEL=Web platform: settings, deep links, and admin surfaces",
  "python3",
  "/opt/qa-harness/harness/stage.py",
]

[[run.tasks]]
name = "qa-agent-cli-cross"

[[run.tasks.steps]]
name = "checkout-prompts"
exec = [
  "bash",
  "-lc",
  '''
set -euo pipefail
DEST=/workspace/.islo-prompts/REPLACE_WITH_REPOSITORY
git config --global url."https://x-access-token:$GH_TOKEN@github.com/".insteadOf "https://github.com/"
if [ -d "$DEST/.git" ]; then
  git -C "$DEST" fetch --depth 1 origin HEAD
  git -C "$DEST" checkout -f FETCH_HEAD
else
  mkdir -p "$(dirname "$DEST")"
  git clone --depth 1 https://github.com/REPLACE_WITH_OWNER/REPLACE_WITH_REPOSITORY.git "$DEST"
fi
''',
]

[[run.tasks.steps]]
name = "explore"
workdir = "/workspace/qa-harness"

[run.tasks.steps.run_agent]
mode = "session"
harness = "claude"
model = "claude-sonnet-4-5"
prompt = { type = "literal", value = """
Read and follow /workspace/.islo-prompts/REPLACE_WITH_REPOSITORY/.islo/prompts/cli-cross.md.
""" }

[[run.tasks.steps]]
name = "stage-findings"
exec = [
  "env",
  "QA_AGENT_ID=qa-agent-cli-cross",
  "QA_BRIEF_LABEL=CLI and cross-surface consistency",
  "python3",
  "/opt/qa-harness/harness/stage.py",
]
